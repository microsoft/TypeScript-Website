// @esModuleInterop
import express from "express"
const app = express()

app.get("/", function (req, res) {
  res.se /*** @completions */
})

app.listen(3000)

function normalizeText(lines: string[]) {
  for (const line of lines) {
    line = line.tri /* @completions {{ filter: prefix(trim) }} */ // trimLeft, trimRight, trim
  }
}
