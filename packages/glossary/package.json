{
  "name": "glossary",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "generate-markdown": "pnpm ts-node --project tsconfig.json scripts/generateMarkdown.ts ",
    "build": "pnpm generate-markdown",
    "test": "pnpm build && pnpm lint",
    "lint": "node scripts/lint.js"
  },
  "prettier": {
    "printWidth": 100,
    "semi": true
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
